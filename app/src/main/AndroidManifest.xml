<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Hardware Features ===== -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- ===== Permissions ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Location (for beacon proximity + GPS verification) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ===== Bluetooth (modern + legacy) ===== -->
    <!-- Legacy support (Android 11 and below) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Modern Bluetooth permissions (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" /><!--android:usesPermissionFlags="neverForLocation"-->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Foreground service (for teacher beacon advertiser) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ===== Application ===== -->
    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/attendancee"
        android:label="ProxyFail"
        android:theme="@style/Theme.ProxyFail"
        android:usesCleartextTraffic="false"
        tools:ignore="MissingClass,MissingPrefix">

        <!-- Teacher Dashboard -->
        <activity
            android:name=".TeacherDashboardActivity"
            android:exported="false" />

        <!-- Student Dashboard -->
        <activity
            android:name=".StudentDashboardActivity"
            android:exported="false" />

        <!-- Attendance (scanner + submission) -->
        <activity
            android:name=".AttendanceActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:screenOrientation="portrait" />

        <!-- Legacy Teacher Activity -->
        <activity
            android:name=".TeacherActivity"
            android:exported="false" />

        <!-- QR Scanner -->
        <activity
            android:name=".QrCodeScannerActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- ZXing Capture Activity -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            tools:replace="screenOrientation" />

        <!-- Login Activity (Launcher) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Register -->
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />

        <!-- Calendar Notification Activity -->
        <activity
            android:name=".CalendarNotificationActivity"
            android:exported="false" />

        <!-- Teacher attendance review (added to fix ActivityNotFoundException) -->
        <activity
            android:name=".TeacherAttendanceReviewActivity"
            android:exported="false" />

        <!-- Scheduled Notifications Activity -->
        <activity
            android:name=".ScheduledNotificationsActivity"
            android:exported="false" />

        <!-- Beacon advertising foreground service (Teacher mode) -->
        <service
            android:name=".BeaconAdvertiserService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|dataSync"
            tools:ignore="ForegroundServicePermission" />

        <!-- Firebase Cloud Messaging Service -->
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Notification Receiver -->
        <receiver
            android:name=".NotificationReceiver"
            android:exported="false" />
    </application>
</manifest>
